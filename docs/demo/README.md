# Demo Material

## Current Recording Scope: 10 September

The scripts in this directory, including the preserved root-only keynote v2/v3/v4
drafts, are narrative donors, not live results or the current acceptance contract.
Keep their wording for traceability. The 10 September `DECISIONS.md` entry,
`PROJECT.md` and framing amendment supersede their older separate-agent-email,
publish-only fallback, optional initial email and universal-client claims.

For the candidate recording, use this sequence instead of the older sending scenes:

1. Use a verified MCP client with private workspace authentication. Public discovery is not sign-in; generated-MCP per-user credential injection remains unverified. The recorded gateway has eleven Payr operations without context/void; local upstream has twelve including context. The optional direct-MCP plugin is archived, not a demo prerequisite. Keep all secrets out of chat and footage.
2. Show a confirmed draft, exact version/defaults/client changes, payout and both client/issuer email addresses. Obtain explicit Publish & Send approval; the request must include `approval:true` and `deliveryApproval:true`. This is not spending approval.
3. Show Payr's approved initial emails, frozen PDF attachments and private invoice/PDF links only after actual provider/read-back and inbox checks. Distinct normalized addresses receive separate messages; equal addresses get one combined-role copy. Do not ask the agent to send a duplicate through Gmail or claim two inboxes from the historical same-address test. Provider `sent` means acceptance, not inbox delivery.
4. Pay a fresh, separately approved invoice with the client's funded Arc Testnet wallet. Verify the actual configured event and settlement-derived Paid state, receipt page/PDF/hash and explorer facts. The guarded local-testnet payment attestor is distinct from the Privy receiving wallet; no agent signing or spending is demonstrated.
5. Show receipt delivery only after its separate operator enablement and verification. Initial-email, payment and receipt proof remain core acceptance. If invoice email is disabled, fresh publication is blocked; a retained link-only/prior-transaction fallback must be labeled historical evidence and does not pass this fresh journey.

The recorded 10 September Privy deployment opened the login sheet without real
user credentials. Live ownership/linking/genuine policy denial remain unproven;
do not narrate those as completed from a wallet address or invalid-token denial.
Use `../ops/privy-onboarding.md` and `../ops/mcp-onboarding.md` for dated evidence.
Fresh combined CI/review and approved live gates precede the final recording;
this reconciliation performs no deployment, email send or payment.

## Preserved Drafts

- `payr-keynote-demo-script.md`, `payr-demo-use-case.md` and `payr-demo-video-guide.md`: retained main demo material, not overwritten by root donors.
- `payr-keynote-demo-script-v2.md`: preserved root personal-hook draft; its email-disabled sharing fallback is superseded above.
- `payr-keynote-demo-script-v3.md`: preserved root narrative draft; universal tool-client/logo and automatic accounting claims are not verified integrations or current claims.
- `payr-keynote-demo-script-v4.md`: preserved root agent-first draft; separate connected-email-tool approval and no-attachment/no-issuer-copy wording are superseded above.

None of these scripts authorizes payment, mail or real business details. Use only
approved or fictional display data and verified client compatibility. The final
2-4 minute video may include narration around the separately timed under-three-
minute core journey; neither timing is established by a script alone.

These scripts are recording plans, not evidence of completed live acceptance.
Use a freshly approved amount and label the recording as an Arc Testnet demo;
illustrative 250 USDC examples do not authorize a payment or email.

Show only the payment-link domain. Redact bearer tokens and usable QR codes
from browser bars, connector output, screenshots, and recordings, and use
synthetic billing profiles. Billing contents stay offchain and are accessible
to authorized workspace users and anyone holding the bearer link. Settlement
terms, addresses, invoice key, and document commitment are public onchain.

## Architecture Reference

Both the user-owned SVG and any root `ARCHITECTURE` placeholder remain untouched.

The user-owned `../architecture.excalidraw.svg` is preserved unchanged as a
historical concept, not the current implemented architecture. Financing,
liquidity pools, credit scoring, auditing, tokenized collateral, and quoted
rates are illustrative only and are not available product features. The
financing arithmetic has not been validated and must not be used as financial
guidance. Email delivery remains operator-gated; unattended production
delivery is not proven. Do not submit this diagram as the final architecture
without the owner's review and correction of both visible and embedded source.
